{{- include "ffc-helm-library.deployment" (list . "ffc-sfi-payments.deployment") -}}
{{- define "ffc-sfi-payments.deployment" -}}
spec:
  template:
    spec:
      containers:
      - {{ include "ffc-helm-library.container" (list . "ffc-sfi-payments.container") }}
{{- end -}}
