jest.mock('../../../app/data')
const mockDb = require('../../../app/data')

const saveInvoiceLines = require('../../app/inbound/save-invoice-lines')

describe('save invoice lines', () => {
  beforeEach(() => {

  })

  afterEach(() => {
    jest.clearAlllMocks()
  })

  test('should call saveInvoiceLines...', async () => {

  })
})
