const acknowledged = require('./mock-acknowledged')
const frn = require('./mock-frn')
const invoiceNumber = require('./mock-invoice-number')
const success = require('./mock-success')

module.exports = {
  invoiceNumber,
  frn,
  success,
  acknowledged
}
